# Bambu Bridge — local agent dependencies (Python 3.10+)
paho-mqtt==2.1.0         # MQTT session to Bambu printers; pinned, see bridge/bambu/session.py
httpx>=0.27              # outbound HTTPS client to the 3DPF bridge API
tomli>=2.0; python_version < "3.11"   # config parsing (tomllib is stdlib on 3.11+)

# dev
pytest>=7.4
