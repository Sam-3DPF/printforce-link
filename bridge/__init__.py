"""PrintForce Link — the local agent that connects 3D PrintForce to Bambu printers.

The internal Python package keeps the name `bridge` (its endpoints, tests, and the
`bambu-bridge` feature flag all speak it); the product/repo is PrintForce Link.
"""

__version__ = "0.1.1"
